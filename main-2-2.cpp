#include <stdio.h>
#include <function-2-2.cpp>
int main()
{
    int binary_digits[5] = [10011];
    int number_of_digits = sizeof(binary_digits);
    int bin_to_int(int binary_digits[], int number_of_digits);

    return 0;
}
